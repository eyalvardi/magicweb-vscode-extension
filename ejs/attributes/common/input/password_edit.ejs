<% if (ctrl.props.password_edit && (ctrl.style && !ctrl.style.multiline_edit)) { %>
    <% if (ctrl.props.password_edit.has_exp){ %>
        [type]="mg.getType('<%= ctrl.props.id %>')"
    <% } else { %>
        type="password"
    <% } %>
<% } %>
